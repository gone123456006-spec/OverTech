import { Orders } from './Orders';

/**
 * Dedicated admin panel wrapper.
 * This is intentionally separated as its own file/entry component.
 */
export function AdminOrderDashboard() {
  return <Orders />;
}
